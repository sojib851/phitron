#include <stdio.h>

int main(){

    int n;
    scanf("%d", &n);

    int day = 0, month = 0, years = 0;

    for (int i = 0; i < n; i++)
    {
        if (i <= n)
        {
            day++;

            if (i == 30)
            {
                /* code */
            }
            
        }
        
    }
    

    return 0;
}