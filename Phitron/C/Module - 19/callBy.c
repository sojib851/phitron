Call by Value এবং Call by Reference প্রোগ্রামিং এ দুটি ভিন্ন মেমোরি পাসিং কনসেপ্ট, যেখানে ফাংশন কিভাবে প্যারামিটার গ্রহণ করে এবং তার উপর কার্যক্রম সম্পাদনা করে তা ব্যাখ্যা করা হয়। আসুন এগুলো বিস্তারিত দেখি:

1. Call by Value (মান দ্বারা কল)
সংজ্ঞা: Call by Value এর ক্ষেত্রে ফাংশনে আর্গুমেন্ট পাস করার সময় শুধুমাত্র তার মান (value) কপি করা হয় এবং এই কপি করা মান ফাংশনের নিজস্ব লোকাল ভেরিয়েবলে সংরক্ষিত থাকে।

কাজের ধরন:

মূল ভেরিয়েবলের মান পরিবর্তন হয় না। কারণ ফাংশনে পাঠানো মানটি কেবল কপি হয়, এবং মূল ভেরিয়েবলের সাথে এর কোনো সরাসরি সংযোগ থাকে না।
কোনো পরিবর্তন ফাংশনের ভেতরেই সীমাবদ্ধ থাকে এবং বাইরে প্রভাবিত হয় না।
উদাহরণ:

c
Copy code
#include <stdio.h>

void changeValue(int a) {
    a = 10; // কেবল লোকাল ভেরিয়েবল 'a' পরিবর্তন হচ্ছে
}

int main() {
    int x = 5;
    changeValue(x);
    printf("%d", x); // আউটপুট: 5, কারণ মূল ভেরিয়েবল x পরিবর্তিত হয়নি
    return 0;
}
Call by Value এর ক্ষেত্রে মূল ভেরিয়েবল (x) অপরিবর্তিত থাকে।

2. Call by Reference (রেফারেন্স দ্বারা কল)
সংজ্ঞা: Call by Reference এর ক্ষেত্রে ফাংশনে আর্গুমেন্ট পাস করার সময় ভেরিয়েবলের রেফারেন্স (অবস্থান) বা ঠিকানা পাঠানো হয়, ফলে ফাংশন সরাসরি মূল ভেরিয়েবলের সাথে কাজ করতে পারে।

কাজের ধরন:

মূল ভেরিয়েবলের মান পরিবর্তন হয়, কারণ ফাংশনের ভেতরের পরিবর্তন সরাসরি মূল ভেরিয়েবলে প্রতিফলিত হয়।
রেফারেন্স পাঠানোর জন্য সাধারণত পয়েন্টার (pointer) ব্যবহার করা হয় সি প্রোগ্রামিং ভাষায়, যেখানে ভেরিয়েবলের ঠিকানা সরাসরি ফাংশনে পাঠানো হয়।
উদাহরণ:

c
Copy code
#include <stdio.h>

void changeValue(int *a) {
    *a = 10; // মূল ভেরিয়েবল পরিবর্তিত হচ্ছে কারণ তার ঠিকানায় কাজ হচ্ছে
}

int main() {
    int x = 5;
    changeValue(&x); // মূল ভেরিয়েবলের ঠিকানা পাঠানো হচ্ছে
    printf("%d", x); // আউটপুট: 10, কারণ মূল ভেরিয়েবল x পরিবর্তিত হয়েছে
    return 0;
}
এখানে মূল ভেরিয়েবল (x) এর মান পরিবর্তিত হয় কারণ তার ঠিকানায় সরাসরি পরিবর্তন আনা হয়েছে।

Call by Value এবং Call by Reference এর পার্থক্য:
বৈশিষ্ট্য	Call by Value	Call by Reference
আর্গুমেন্ট পাসিং	কেবল মান পাস করা হয়	ঠিকানা বা রেফারেন্স পাস করা হয়
মূল ভেরিয়েবলের মান পরিবর্তন	হয় না	হয়
প্রভাব	ফাংশনের ভেতরে সীমাবদ্ধ	মূল ভেরিয়েবলের ওপর সরাসরি প্রভাব পড়ে
ভাষা সমর্থন	সি, জাভা ইত্যাদি (বাই ডিফল্ট ভ্যালু)	সি++, পাসিং পয়েন্টারের মাধ্যমে সি-তে
সি প্রোগ্রামিং এ ব্যবহার:
সি প্রোগ্রামিংয়ে সাধারণত Call by Reference করতে পয়েন্টার ব্যবহার করা হয়, কারণ এতে মূল ভেরিয়েবলের ঠিকানা পাস করে সরাসরি মূল ডেটা পরিবর্তন করা সম্ভব হয়।

আশা করি, এই ব্যাখ্যাটি Call by Value এবং Call by Reference এর মধ্যে পার্থক্য এবং কাজের ধরন স্পষ্টভাবে তুলে ধরেছে।