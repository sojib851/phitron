#include<bits/stdc++.h>
using namespace std;

int main()
{
    // int n;
    // cin >> n;
    // int a[n];

    // for (int i = 0; i < n; i++)
    // {
    //     cin >> a[i];
    // }
    
    // reverse(starting a, end a + n); reverse the number of array;

    // for (int i = 0; i < n; i++)
    // {
    //     cout << a[i] << " "; 
    // }
    
    string s;
    getline(cin, s);
    
    reverse(s.begin(), s.end());

    cout << s << endl;
stringstream

    return 0;
}